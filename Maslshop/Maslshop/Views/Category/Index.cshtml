@model Maslshop.Models.ViewModels.CategoriesViewModel
@{
    ViewBag.Title = Model.Heading;
}

<h2>@Model.Heading</h2>


<p><a href="@Url.Action("Add", "Category")" class="btn btn-primary" role="button">Dodaj kategorię</a></p>

<table id="table" class="table table-striped table-bordered text-center">
    <thead>
    <tr class="active text-info">
        <th>Id</th>
        <th>Nazwa</th>
        <th>Ilość Produktów</th>
        <th>Edycja</th>
        <th>Usuń</th>      
    </tr>
    </thead>
    <tbody class="text-center">
    @foreach (var category in Model.Categories)
    {
        <tr>
            <td>@category.Id</td>
            <td><a href="@Url.Action("Index", "Product", new {query = category.Name} )">@category.Name</a></td>
            <td>
                @category.Products.Count
            </td>
            <td><a href="@Url.Action("Edit", "Category", new {categoryId = category.Id})" class="btn btn-primary" role="button">Edytuj</a></td>
            <td><a href="@Url.Action("Delete", "Category", new {categoryId = category.Id})" class="btn btn-primary" role="button">Usuń</a></td>
        </tr>
    }
    </tbody>
</table>
