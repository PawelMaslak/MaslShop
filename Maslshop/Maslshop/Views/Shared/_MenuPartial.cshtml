
@if (User.Identity.IsAuthenticated)
{

    <ul class="nav navbar-nav">
        @if (!User.IsInRole("Administrator"))
        {
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Narzędzia Użytkownika<span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="@Url.Action("UserOrders", "Order")">My Orders</a></li>
                </ul>
            </li>
        }
        @if (User.IsInRole("Administrator"))
        {
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Narzędzia Administratora<span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="@Url.Action("Index", "Admin")">Panel Administracyjny</a></li>
                    <li role="separator" class="divider">Tools</li>
                    <li><a href="@Url.Action("Index", "Roles")">Kreator Ról</a></li>
                    <li role="separator" class="divider">Products</li>
                    <li><a href="@Url.Action("Index", "Category")">Kategorie</a></li>
                    <li><a href="@Url.Action("Index", "Product")">Lista produktów</a></li>
                    <li role="separator" class="divider">Orders</li>
                    <li><a href="@Url.Action("ViewOrders", "Order")">Zamówienia</a></li>
                    <li><a href="@Url.Action("Deliveries", "Delivery")">Przesyłki</a></li>
                </ul>
            </li>
        }
    </ul>
}